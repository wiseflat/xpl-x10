#!upstart
description "wiseflat xpl-x10"
author      "Mathieu Garcia"

start on startup
stop on shutdown

script
    echo $$ > /var/run/xpl-x10.pid
    exec sudo -u root /usr/bin/node /opt/xpl-x10/index.js >> /var/log/xpl-x10.log 2>&1
end script

pre-start script
    # Date format same as (new Date()).toISOString() for consistency
    echo "[`date -u +%Y-%m-%dT%T.%3NZ`] (sys) Starting" >> /var/log/xpl-x10.log
end script

pre-stop script
    rm /var/run/xpl-x10.pid
    echo "[`date -u +%Y-%m-%dT%T.%3NZ`] (sys) Stopping" >> /var/log/xpl-x10.log
end script
